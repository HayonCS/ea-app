query GetComboPerformanceRowsDateRange(
  $start: String!
  $end: String!
  $assetIds: [Int!]
  $partIds: [Int!]
  $operatorIds: [Int!]
) {
  comboPerformanceRowsDateRange(
    start: $start
    end: $end
    assetIds: $assetIds
    partIds: $partIds
    operatorIds: $operatorIds
  )
}
