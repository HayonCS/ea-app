query GetTestPlanRevisions($domain: Domain!, $where: TestPlanWhereInput!) {
  testPlanRevisions(domain: $domain, where: $where) {
    revs {
      RevNumber
      LastEditor
      LastEdit
      RevLabel
      LogMessage
    }
  }
}
