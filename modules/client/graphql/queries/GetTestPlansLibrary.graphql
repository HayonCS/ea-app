query GetTestPlansLibrary($domain: Domain!) {
  testPlanLibrary(domain: $domain) {
    testPlanLib {
      PlanTitle
      Type
      Version
      LastEditor
      LastEdit
      RevLabel
    }
  }
}
