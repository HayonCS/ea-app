import { AppSelectors } from "client/redux/selectors/app-selectors";
import { WebdcSelectors } from "client/redux/selectors/webdc-selectors";

export const Selectors = {
  App: AppSelectors,
  Webdc: WebdcSelectors,
};
